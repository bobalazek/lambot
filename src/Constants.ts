import path from 'path';

export const ID_PREFIX = 'LAMBOT_';
export const BUILD_DIR = path.resolve(__dirname, '..', 'build');
export const DATA_SESSIONS_DIR = path.resolve(__dirname, '..', 'data', 'sessions');
